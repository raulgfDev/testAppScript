
function unaFuncion () {
  console.log(`Hello ${ mundo }`);
}

// arrow function, no exe like library ( biblioteca )
const dosFuncion = () => {
  console.log('esto es la funcion dos');
}

/**
 *
 * @param { number } dividendo sera el dividendo
 * @param { number } divisor sera el divisor
 *
 * @return { number } sera el resultado del la division
 * 
 * @customfunctions
 * 
 */
function division ( dividendo, divisor ) {
  return dividendo / divisor;
}
